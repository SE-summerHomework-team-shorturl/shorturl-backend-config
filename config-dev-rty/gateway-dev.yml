spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedMethods: "*"
            allowedHeaders: "*"
            allowedOrigins: "*"
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: gateway
        healthCheckPath: /actuator/health
        healthCheckInterval: 5s
        health-check-critical-timeout: 20s
        instanceId: gateway:8080
        enabled: true
        prefer-ip-address：true
management:
  endpoints:
    web:
      exposure:
        include: "*"
